compact: true

framework:
  - terraform

skip-path:
  - .terraform

skip-check:
  - CKV2_AWS_62   # S3 event notifications are not required for this dev bucket
  - CKV_AWS_18    # S3 access logging is not required for this dev bucket
  - CKV_AWS_144   # Cross-region replication is not required for this dev bucket
  - CKV_AWS_109   # KMS key administration policy is intentionally defined
  - CKV_AWS_111   # KMS key administration policy is intentionally defined
  - CKV_AWS_356   # KMS key policy uses Resource "*" by design

 # Cross-region DR not required for dev